<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4"
      main_tree_to_execute="NavigatorSequence">
  <BehaviorTree ID="CancelAllNavigation">
    <Sequence>
      <ForceSuccess>
        <CancelHover name="CancelHoverBeforeGotoPose"
                     server_timeout="1000"
                     server_name="hover"/>
      </ForceSuccess>
      <ForceSuccess>
        <CancelFollowPath server_timeout="1000"
                          server_name="follow_path"/>
      </ForceSuccess>
    </Sequence>
  </BehaviorTree>

  <!-- ExitHoverForNav: leave any active hover before nav, but DO NOT cancel
       the live FollowPath. Used on entry to NavigateThroughWaypoints and
       SurveyLine so the next FollowPath goal preempts the active one at the
       follow_path server (no uncommanded zero-cmd window). CancelAllNavigation
       is still used for GotoPose, which is an operator hard-stop and *should*
       drop FollowPath. For #28. -->
  <BehaviorTree ID="ExitHoverForNav">
    <ForceSuccess>
      <CancelHover name="CancelHoverBeforeNav"
                   server_timeout="1000"
                   server_name="hover"/>
    </ForceSuccess>
  </BehaviorTree>

  <BehaviorTree ID="GotoPose">
    <Sequence name="GotoPoseSequence">
      <SubTree ID="NavigateThroughWaypoints"
               error_code_id="{navigate_through_poses_error_code}"
               server_timeout="{server_timeout}"
               wait_for_service_timeout="{wait_for_service_timeout}"
               bt_loop_duration="{bt_loop_duration}"
               behavior_tree=""
               goals="{goal_poses}"
               _autoremap="true"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="GotoTask">
    <ReactiveSequence name="GotoTaskReactiveSequence">
      <ScriptCondition code="current_task_type == &apos;goto&apos;"/>
      <Sequence name="GotoPoseAndSetDoneSequence">
        <SetPathFromTask end_index="-1"
                         path="{goal_poses}"
                         start_index="0"
                         task="{current_task}"/>
        <SubTree ID="GotoPose"
                 _autoremap="true"/>
        <SetTaskDone name="SetGotoTaskDone"
                     task="{current_task}"/>
      </Sequence>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="HoverTask">
    <ReactiveSequence>
      <ScriptCondition code="current_task_type == &apos;hover&apos;"/>
      <!-- RestartOnTaskChange forces a clean re-entry of the hover Sequence
           whenever {current_task_update_time} changes (a new or updated hover
           task). The Switch that dispatches HoverTask keys on task *type*, so a
           same-type re-command (a new hover_override, or a different hover id)
           does NOT halt this branch on its own; without this decorator the
           Sequence would stay parked at the RUNNING Hover child and never
           recompute the target. Task::update bumps current_task_update_time only
           on a real message change, so benign feedback re-sends don't restart.
           For #46. -->
      <RestartOnTaskChange task_update_time="{current_task_update_time}">
        <!-- Plain Sequence (NOT SequenceWithMemory): Sequence::halt() resets the
             child index to 0, so every entry re-runs the goal-set Fallback and
             PredictStoppingPose for a fresh stop point, while a continuous hold
             still resumes at the RUNNING Hover child (no per-tick recompute that
             would chase the drifting boat). SequenceWithMemory kept its index
             across halts and — because Hover never returns SUCCESS — never
             re-ticked PredictStoppingPose, freezing {hover_target} at the first
             value ever computed (#46). Continuous-hold safety relies on the
             enclosing RestartOnTaskChange decorator (this Sequence's parent)
             staying the LAST child of the outer ReactiveSequence: a sibling
             added after it would make the ReactiveSequence halt this subtree
             every tick, recomputing the stop point and chasing the boat. -->
        <Sequence>
          <Fallback name="SetHoverGoalFallback">
            <Inverter>
              <SetPathFromTask end_index="-1"
                               path="{goal_poses}"
                               start_index="0"
                               task="{current_task}"/>
            </Inverter>
            <IfThenElse>
              <PathEmptyCondition path="{goal_poses}"/>
              <AlwaysSuccess/>
              <SubTree ID="GotoPose"
                       _autoremap="true"/>
            </IfThenElse>
          </Fallback>
          <!-- Hold the momentum-coasting stop point instead of the live pose, so
               a boat arriving at survey speed decelerates into the point rather
               than overshooting and driving back (#33). After a transit (location
               given) the boat is ~stopped so this ≈ the current pose. Recomputed
               once per entry (see Sequence note above) to avoid a stale target
               leaking from a previous hover. -->
          <PredictStoppingPose deceleration="{default_deceleration}"
                               pose="{hover_target}"/>
          <Script code="navigation_state := &apos;hover&apos;"/>
          <Hover target="{hover_target}"
                 maximum_radius="0.000000"
                 maximum_speed="0.000000"
                 error_code_id="{hover_error_code}"
                 server_timeout="1000"
                 minimum_radius="0.000000"
                 server_name="hover"/>
        </Sequence>
      </RestartOnTaskChange>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="NavigateThroughWaypoints">
    <Sequence>
      <!-- ExitHoverForNav (CancelHover only) instead of CancelAllNavigation,
           so the live FollowPath is not pre-emptively canceled here. The next
           FollowPath goal will preempt the active one at the follow_path
           server, eliminating the uncommanded zero-cmd window during line
           transitions that previously tripped the FCU's 3 s GUIDED watchdog
           (#28; field trip 2026-05-22). -->
      <SubTree ID="ExitHoverForNav"
               _autoremap="true"/>
      <ClearPath navigation_path="{path}"/>
      <FixPathOrientations output_path="{goals}"
                           robot_frame="{robot_frame}"
                           tf_buffer="{tf_buffer}"
                           input_path="{goals}"/>
      <PathToPoseVector poses="{goals_vector}"
                        path="{goals}"/>
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector topic_name="controller_selector"
                            default_controller="FollowPath"
                            selected_controller="{selected_controller}"/>
        <PlannerSelector topic_name="planner_selector"
                         default_planner="GridBased"
                         selected_planner="{selected_planner}"/>
        <!-- hz raised from 0.5 → 2.0 so re-plan/refresh cadence is well under
             the FCU's 3 s GUIDED watchdog margin (#28). num_attempts on
             ComputePathThroughPoses capped 20 → 4 so a stuck planner can't
             stack retries past the watchdog — fail loudly to SetTaskFailed
             rather than stall silently. -->
        <RateController hz="2.0">
          <Fallback name="ComputePathThroughPoses">
            <ReactiveSequence>
              <RemovePassedGoals input_goals="{goals_vector}"
                                 radius="2.5"
                                 global_frame=""
                                 robot_base_frame=""
                                 output_goals="{goals_vector}"/>
              <IsPathValid path="{path}"
                           server_timeout="{server_timeout}"/>
            </ReactiveSequence>
            <RetryUntilSuccessful num_attempts="4">
              <ComputePathThroughPoses goals="{goals_vector}"
                                       server_name="compute_path_through_poses"
                                       server_timeout="{server_timeout}"
                                       planner_id="{selected_planner}"
                                       path="{path}"
                                       error_code_id="{compute_path_error_code}"/>
            </RetryUntilSuccessful>
          </Fallback>
        </RateController>
        <SetControllerSpeed speed="{target_speed}"
                            controller_name="{selected_controller}"/>
        <FollowPath controller_id="{selected_controller}"
                    path="{path}"
                    goal_checker_id=""
                    progress_checker_id=""
                    server_name="follow_path"
                    server_timeout="{server_timeout}"
                    error_code_id="{follow_path_error_code}"/>
      </PipelineSequence>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="NavigatorSequence">
    <ReactiveSequence name="MainRunSequence">
      <SubTree ID="UpdateCurrentTaskData"
               _autoremap="true"/>
      <!-- Dispatch on task type with an explicit Switch so the two failure shapes are
           structurally distinct (#25): an UNMATCHED type hits the default child
           (SetTaskDone catchall, correct); a MATCHED case that FAILS returns FAILURE and
           does NOT fall through to the default. Each matched case is wrapped
           Fallback[<task>, SetTaskFailed] so a matched-but-failed task is recorded failed
           (status set, mission advances) instead of being silently marked done — or
           aborting the whole run. Switch re-reads {current_task_type} each tick (the
           outer ReactiveSequence re-ticks UpdateCurrentTaskData first), so a mid-run type
           change still halts the running case and re-routes, preserving clear->resend. -->
      <Switch5 name="SelectTask"
               variable="{current_task_type}"
               case_1="hover"
               case_2="goto"
               case_3="survey_line"
               case_4="survey_line_set"
               case_5="survey_area">
        <Fallback name="HoverOrFailed">
          <SubTree ID="HoverTask"
                   wait_for_service_timeout="{wait_for_service_timeout}"
                   server_timeout="{server_timeout}"
                   bt_loop_duration="{bt_loop_duration}"
                   current_task_type="{current_task_type}"
                   navigation_state="{navigation_state}"
                   current_task="{current_task}"
                   current_task_update_time="{current_task_update_time}"
                   _autoremap="true"/>
          <SetTaskFailed name="HoverFailed" task="{current_task}" reason="hover_failed"/>
        </Fallback>
        <Fallback name="GotoOrFailed">
          <SubTree ID="GotoTask"
                   current_task="{current_task}"
                   lead_in_distance="0.0"
                   server_timeout="{server_timeout}"
                   bt_loop_duration="{bt_loop_duration}"
                   wait_for_service_timeout="{wait_for_service_timeout}"
                   _autoremap="true"/>
          <SetTaskFailed name="GotoFailed" task="{current_task}" reason="goto_failed"/>
        </Fallback>
        <Fallback name="SurveyLineOrFailed">
          <SubTree ID="SurveyLineTask"
                   lead_in_distance="{survey_lead_in_distance}"
                   survey_line_task="{current_task}"
                   _autoremap="true"/>
          <SetTaskFailed name="SurveyLineFailed" task="{current_task}" reason="survey_line_failed"/>
        </Fallback>
        <Fallback name="SurveyLineSetOrFailed">
          <SubTree ID="SurveyLineSetTask"
                   survey_line_set_task="{current_task}"
                   _autoremap="true"/>
          <SetTaskFailed name="SurveyLineSetFailed" task="{current_task}" reason="survey_line_set_failed"/>
        </Fallback>
        <Fallback name="SurveyAreaOrFailed">
          <SubTree ID="SurveyAreaTask"
                   survey_area_task="{current_task}"
                   server_timeout="{server_timeout}"
                   bt_loop_duration="{bt_loop_duration}"
                   wait_for_service_timeout="{wait_for_service_timeout}"
                   _autoremap="true"/>
          <SetTaskFailed name="SurveyAreaFailed" task="{current_task}" reason="survey_area_failed"/>
        </Fallback>
        <SetTaskDone name="SkipUnknownTaskType"
                     task="{current_task}"/>
      </Switch5>
      <KeepRunningUntilFailure>
        <Inverter>
          <AllTasksDoneCondition task_list="{task_list}"/>
        </Inverter>
      </KeepRunningUntilFailure>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="RunSurveyAreaSubTasks">
    <ReactiveSequence>
      <GetSubTasks sub_tasks="{survey_area_tasks}"
                   task="{survey_area_task}"/>
      <WhileDoElse>
        <AllTasksDoneCondition task_list="{survey_area_tasks}"/>
        <AlwaysSuccess/>
        <KeepRunningUntilFailure>
          <ReactiveSequence>
            <SubTree ID="UpdateCurrentTaskData"
                     default_planner="{=}"
                     robot_default_speed="{=}"
                     target_speed="{=}"
                     current_tassk="{=}"
                     active_task_id="{=}"
                     current_task="{current_survey_area_task}"
                     planner="{=}"
                     current_task_type="{current_survey_area_task_type}"
                     controller="{=}"
                     current_task_id="{current_survey_area_task_id}"
                     default_controller="{=}"
                     task_list="{survey_area_tasks}"
                     _autoremap="true"/>
            <!-- Dispatch sub-tasks by type with a Switch (mirrors the top-level fix): an
                 UNMATCHED sub-task type hits the default SetTaskDone (skip, not failed) so
                 the _failureIf routing can't conflate a type-mismatch with an execution
                 failure (#25 review). A matched sub-task that fails — FollowPath recovery
                 exhausted, or any other failure in the line subtree (transit, path setup,
                 TF) — routes to SetTaskFailed, which records it failed and marks it done so
                 the area CONTINUES its remaining lines (skip-and-continue) instead of being
                 abandoned. -->
            <Switch2 name="SelectSurveyAreaSubTask"
                     variable="{current_survey_area_task_type}"
                     case_1="survey_line"
                     case_2="survey_line_set">
              <Fallback name="SurveyAreaLineOrFailed">
                <Sequence>
                  <SubTree ID="SurveyLineTask"
                           lead_in_distance="{survey_lead_in_distance}"
                           survey_line_task="{current_survey_area_task}"
                           server_timeout="{server_timeout}"
                           bt_loop_duration="{bt_loop_duration}"
                           wait_for_service_timeout="{wait_for_service_timeout}"
                           current_task_type="{current_survey_area_task_type}"
                           _autoremap="true"/>
                  <SetTaskDone task="{current_survey_area_task}"/>
                </Sequence>
                <SetTaskFailed task="{current_survey_area_task}" reason="survey_line_failed"/>
              </Fallback>
              <Fallback name="SurveyAreaLineSetOrFailed">
                <Sequence>
                  <SubTree ID="SurveyLineSetTask"
                           name="SurveyAreaSurveyLineSetTask"
                           survey_line_set_task="{current_survey_area_task}"
                           current_task_type="{current_survey_area_task_type}"
                           _autoremap="true"/>
                  <SetTaskDone task="{current_survey_area_task}"/>
                </Sequence>
                <SetTaskFailed task="{current_survey_area_task}" reason="survey_line_set_failed"/>
              </Fallback>
              <SetTaskDone name="SkipUnknownSurveyAreaSubTaskType"
                           task="{current_survey_area_task}"/>
            </Switch2>
          </ReactiveSequence>
        </KeepRunningUntilFailure>
      </WhileDoElse>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="SonarCoverageTask">
    <Sequence>
      <SetPolygonFromTask polygon="{survey_area_polygon}"
                          end_index="-1"
                          start_index="0"
                          task="{current_task}"/>
      <ClearPath navigation_path="{current_survey_line}"/>
      <PipelineSequence>
        <IfThenElse>
          <PathEmptyCondition path="{current_survey_line}"/>
          <AlwaysSuccess/>
          <Sequence>
            <IfThenElse>
              <HasSubTasksCondition task_id="{current_line_label}"
                                    task_type=""
                                    task="{current_task}"/>
              <AlwaysSuccess/>
              <AddSubTask sub_task_path="{current_survey_line}"
                          sub_task_type="survey_line"
                          sub_task_id="{current_line_label}"
                          parent_task="{current_task}"/>
            </IfThenElse>
            <ClearPath navigation_path="{current_survey_line}"/>
          </Sequence>
        </IfThenElse>
        <SonarCoverage survey_area="{survey_area_polygon}"
                       current_line="{current_survey_line}"
                       current_line_label="{current_line_label}"
                       server_timeout="1000"
                       current_line_number="{current_line_number}"
                       server_name="compute_sonar_coverage_path"/>
      </PipelineSequence>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="SurveyAreaTask">
    <ReactiveSequence>
      <ScriptCondition code="current_task_type == &apos;survey_area&apos;"/>
      <Sequence>
        <IfThenElse name="NeedCoverageGeneratorCheck">
          <HasSubTasksCondition task_id=""
                                task_type=""
                                task="{survey_area_task}"/>
          <SubTree ID="RunSurveyAreaSubTasks"
                   name="RunSurveyAreaSubTasksWithoutCoverageGenerator"
                   _autoremap="true"/>
          <PipelineSequence>
            <SubTree ID="RunSurveyAreaSubTasks"
                     name="RunSurveyAreaSubTasksWithCoverageGenerator"
                     _autoremap="true"/>
            <SubTree ID="SonarCoverageTask"
                     _autoremap="true"/>
          </PipelineSequence>
        </IfThenElse>
        <SetTaskDone name="SetSurveyAreaTaskDone"
                     task="{survey_area_task}"/>
      </Sequence>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="SurveyLine">
    <Sequence>
      <!-- ExitHoverForNav (CancelHover only) instead of CancelAllNavigation:
           SurveyLine entry must not pre-emptively cancel the transit FollowPath
           that just started — the next FollowPath goal (survey_path) preempts
           it cleanly at the follow_path server. See #28. -->
      <SubTree ID="ExitHoverForNav"
               _autoremap="true"/>
      <ReactiveSequence>
        <Script code="navigation_state := &apos;survey_line&apos;"/>
        <ControllerSelector topic_name="controller_selector"
                            default_controller="FollowPath"
                            selected_controller="{selected_controller}"/>
        <SetControllerSpeed speed="{target_speed}"
                            controller_name="{selected_controller}"/>
        <!-- Refresh {nominal_survey_path} every tick so an operator mid-line mission
             re-send (which updates {current_task} → {survey_line_task} via
             UpdateCurrentTaskData upstream) propagates to FollowPath. Nav2
             FollowPathAction::on_wait_for_result re-reads the path port each
             RUNNING tick, compares against goal_.path, and sets goal_updated_
             on a change → BtActionNode preempts with the new goal. For #35. -->
        <SetPathFromTask end_index="-1"
                         path="{nominal_survey_path}"
                         start_index="0"
                         task="{survey_line_task}"/>
        <!-- Reshape the trackline around obstacles before it reaches FollowPath:
             corridor solver over the local costmap, anchored to the nominal line.
             Passes nominal through unchanged when clear or fully blocked. Re-ticked
             here every loop (the ReactiveSequence IS the monitor); a changed
             {survey_path} preempts the running FollowPath via the #35 mechanism
             above. For #30. -->
        <AdjustPathForObstacles nominal_path="{nominal_survey_path}"
                                path="{survey_path}"/>
        <!-- Recover transient FollowPath ABORTs (e.g. a momentary progress-checker trip)
             with a short Wait, then retry — up to 3 times. On persistent failure the
             RecoveryNode FAILs up to the dispatch, which records the line failed
             (SetTaskFailed) rather than silently marking it done (#25). CancelAllNavigation
             above runs once on entry and is intentionally not re-run on retry. The Wait
             behavior is the marine-appropriate recovery (spin/backup are not for boats). -->
        <RecoveryNode number_of_retries="3"
                      name="SurveyLineFollowRecovery">
          <FollowPath controller_id="{selected_controller}"
                      path="{survey_path}"
                      goal_checker_id=""
                      progress_checker_id=""
                      server_name="follow_path"
                      server_timeout="{server_timeout}"
                      error_code_id="{follow_path_error_code}"/>
          <Wait wait_duration="5.0"
                server_name="wait"
                server_timeout="{server_timeout}"/>
        </RecoveryNode>
      </ReactiveSequence>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="SurveyLineSetTask">
    <ReactiveSequence>
      <ScriptCondition code="current_task_type == &apos;survey_line_set&apos;"/>
      <GetSubTasks sub_tasks="{survey_line_set_sub_tasks}"
                   task="{survey_line_set_task}"/>
      <WhileDoElse>
        <AllTasksDoneCondition task_list="{survey_line_set_sub_tasks}"/>
        <SetTaskDone task="{survey_line_set_task}"/>
        <KeepRunningUntilFailure>
          <ReactiveSequence>
            <SubTree ID="UpdateCurrentTaskData"
                     default_planner="{=}"
                     robot_default_speed="{=}"
                     target_speed="{=}"
                     current_tassk="{=}"
                     active_task_id="{=}"
                     current_task="{survey_line_set_sub_task}"
                     planner="{=}"
                     current_task_type="{survey_line_set_sub_task_type}"
                     controller="{=}"
                     current_task_id="{survey_line_set_sub_task_id}"
                     default_controller="{=}"
                     task_list="{survey_line_set_sub_tasks}"
                     _autoremap="true"/>
            <!-- Sub-tasks of a line set are all survey_line, so no type routing is needed
                 here (unlike the survey-area loop). Wrap the line subtree in a Fallback to
                 SetTaskFailed so a sub-line that fails — FollowPath recovery exhausted, or any
                 other failure in the line subtree (transit, path setup, TF) — is recorded
                 failed and the set CONTINUES its remaining lines (skip-and-continue) rather
                 than abandoning (#25). -->
            <Fallback name="SurveyLineSetSubLineOrFailed">
              <Sequence>
                <SubTree ID="SurveyLineTask"
                         name="SurveyLineSetSurveyLineTask"
                         survey_line_task="{survey_line_set_sub_task}"
                         server_timeout="{server_timeout}"
                         bt_loop_duration="{bt_loop_duration}"
                         wait_for_service_timeout="{wait_for_service_timeout}"
                         current_task_type="{survey_line_set_sub_task_type}"
                         _autoremap="true"/>
                <SetTaskDone task="{survey_line_set_sub_task}"/>
              </Sequence>
              <SetTaskFailed task="{survey_line_set_sub_task}" reason="survey_line_failed"/>
            </Fallback>
          </ReactiveSequence>
        </KeepRunningUntilFailure>
      </WhileDoElse>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="SurveyLineTask">
    <ReactiveSequence>
      <ScriptCondition code="current_task_type == &apos;survey_line&apos;"/>
      <Sequence>
        <SetPathFromTask end_index="-1"
                         path="{survey_path}"
                         start_index="0"
                         task="{survey_line_task}"/>
        <SubTree ID="TransitAndSurveyLine"
                 _autoremap="true"/>
        <SetTaskDone task="{survey_line_task}"/>
      </Sequence>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="TransitAndSurveyLine">
    <Sequence>
      <GetSubPath output_path="{transit_path}"
                  end_index="0"
                  start_index="0"
                  input_path="{survey_path}"/>
      <Script code="navigation_state := &apos;transit&apos;"/>
      <SubTree ID="NavigateThroughWaypoints"
               goals="{transit_path}"
               _autoremap="true"/>
      <SubTree ID="SurveyLine"
               server_timeout="{server_timeout}"
               bt_loop_duration="{bt_loop_duration}"
               wait_for_service_timeout="{wait_for_service_timeout}"
               _autoremap="true"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="UpdateCurrentTaskData">
    <Sequence>
      <UpdateCurrentTask current_task_update_time="{current_task_update_time}"
                         current_task_id="{current_task_id}"
                         current_task_type="{current_task_type}"
                         current_task="{current_task}"
                         task_list="{task_list}"
                         _onSuccess="active_task_id := current_task_id"/>
      <GetTaskDataDouble value="{target_speed}"
                         default_value="0.0"
                         key="speed"
                         task="{current_task}"/>
      <GetTaskDataString value="{controller}"
                         default_value="&quot;&quot;"
                         key="controller"
                         task="{current_task}"/>
      <GetTaskDataString value="{planner}"
                         default_value="&quot;&quot;"
                         key="planner"
                         task="{current_task}"/>
    </Sequence>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Action ID="AddSubTask">
      <input_port name="sub_task_path"
                  default="{sub_task_path}"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Path for the new sub-task to add to parent task</input_port>
      <input_port name="sub_task_type"
                  default="{sub_task_type}"
                  type="std::string">Type of new sub-task to add to parent task</input_port>
      <input_port name="sub_task_id"
                  default="{sub_task_id}"
                  type="std::string">ID of new sub-task to add to parent task</input_port>
      <input_port name="parent_task"
                  default="{parent_task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Parent task to add sub-task to</input_port>
    </Action>
    <Condition ID="AllTasksDoneCondition">
      <input_port name="task_list"
                  default="{task_list}"
                  type="std::shared_ptr&lt;marine_nav_tasks::TaskList&gt;">List of tasks to check</input_port>
    </Condition>
    <Action ID="CancelFollowPath">
      <input_port name="server_timeout"
                  type="std::chrono::milliseconds"/>
      <input_port name="server_name"
                  type="std::string">Action server name</input_port>
    </Action>
    <Action ID="CancelHover">
      <input_port name="server_timeout"
                  type="std::chrono::milliseconds"/>
      <input_port name="server_name"
                  type="std::string">Action server name</input_port>
    </Action>
    <Action ID="ClearPath">
      <output_port name="navigation_path"
                   default="{navigation_path}"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Empty shared pointer to a path to follow</output_port>
    </Action>
    <Action ID="ComputePathThroughPoses">
      <input_port name="goals">Destinations to plan through</input_port>
      <input_port name="server_name">Server name</input_port>
      <input_port name="server_timeout">Server timeout</input_port>
      <input_port name="planner_id">Mapped name to the planner plugin type to use</input_port>
      <output_port name="path">Path created by ComputePathToPose node</output_port>
      <output_port name="error_code_id">"Compute path through poses error code"</output_port>
    </Action>
    <Action ID="ControllerSelector">
      <input_port name="topic_name">Name of the topic to receive controller selection commands</input_port>
      <input_port name="default_controller">Default controller of the controller selector</input_port>
      <output_port name="selected_controller">Name of the selected controller received from the topic subcription</output_port>
    </Action>
    <Action ID="FixPathOrientations">
      <output_port name="output_path"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Path with fixed orientations as nav_msgs/msg/Path</output_port>
      <input_port name="robot_frame"
                  default="{robot_frame}"
                  type="std::string">Robot base frame</input_port>
      <input_port name="tf_buffer"
                  default="{tf_buffer}"
                  type="std::shared_ptr&lt;tf2_ros::Buffer&gt;">TF2 buffer</input_port>
      <input_port name="input_path"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Input path as nav_msgs/msg/Path</input_port>
    </Action>
    <Action ID="FollowPath">
      <input_port name="controller_id"
                  default="FollowPath"/>
      <input_port name="path">Path to follow</input_port>
      <input_port name="goal_checker_id">Goal checker</input_port>
      <input_port name="progress_checker_id">Progress checker</input_port>
      <input_port name="server_name">Server name</input_port>
      <input_port name="server_timeout">Server timeout</input_port>
      <output_port name="error_code_id">Follow Path error code</output_port>
    </Action>
    <Action ID="GetSubPath">
      <output_port name="output_path"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Sub-path as nav_msgs/msg/Path</output_port>
      <input_port name="end_index"
                  default="-1"
                  type="int">Index of the last pose in the trajectory</input_port>
      <input_port name="start_index"
                  default="0"
                  type="int">Index of the first pose in the trajectory</input_port>
      <input_port name="input_path"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Input path as nav_msgs/msg/Path</input_port>
    </Action>
    <Action ID="GetSubTasks">
      <output_port name="sub_tasks"
                   default="{current_sub_tasks}"
                   type="std::shared_ptr&lt;marine_nav_tasks::TaskList&gt;">Sub tasks of the input task</output_port>
      <input_port name="task"
                  default="{current_task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to get sub tasks from</input_port>
    </Action>
    <Action ID="GetTaskDataDouble">
      <output_port name="value"
                   type="double"/>
      <input_port name="default_value"
                  type="double"/>
      <input_port name="key"
                  type="std::string"/>
      <input_port name="task"
                  default="{task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to get data from</input_port>
    </Action>
    <Action ID="GetTaskDataString">
      <output_port name="value"
                   type="std::string"/>
      <input_port name="default_value"
                  type="std::string"/>
      <input_port name="key"
                  type="std::string"/>
      <input_port name="task"
                  default="{task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to get data from</input_port>
    </Action>
    <SubTree ID="GotoTask"
             editable="true">
      <input_port name="current_task"
                  default="{=}">Task with a goal pose as it's first pose element.</input_port>
    </SubTree>
    <Condition ID="HasSubTasksCondition">
      <input_port name="task_id"
                  type="std::string">Optional ID of tasks to check</input_port>
      <input_port name="task_type"
                  type="std::string">Optional type of tasks to check</input_port>
      <input_port name="task"
                  default="{current_task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to check for sub-tasks</input_port>
    </Condition>
    <Action ID="Hover">
      <input_port name="target"
                  type="geometry_msgs::msg::PoseStamped_&lt;std::allocator&lt;void&gt; &gt;">Pose to hold station at; an empty header.frame_id means hold the current pose</input_port>
      <input_port name="maximum_radius"
                  default="0.000000"
                  type="double">Radius at which robot uses maximum speed to get to the target</input_port>
      <input_port name="maximum_speed"
                  default="0.000000"
                  type="double">Maximum speed to use to get to the target</input_port>
      <output_port name="error_code_id"
                   type="unsigned short">The hover server error code</output_port>
      <input_port name="server_timeout"
                  type="std::chrono::milliseconds"/>
      <input_port name="minimum_radius"
                  default="0.000000"
                  type="double">Radius within which robot is deemed at the target</input_port>
      <input_port name="server_name"
                  type="std::string">Action server name</input_port>
    </Action>
    <Condition ID="IsPathValid">
      <input_port name="path"> Path to validate </input_port>
      <input_port name="server_timeout"> Server timeout </input_port>
    </Condition>
    <Condition ID="PathEmptyCondition">
      <input_port name="path"
                  default="{path}"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Path to check</input_port>
    </Condition>
    <Action ID="PredictStoppingPose">
      <input_port name="deceleration"
                  type="double">Deceleration in m/s^2 (must be &lt; 0) used to project the coasting stop point</input_port>
      <output_port name="pose"
                   type="geometry_msgs::msg::PoseStamped_&lt;std::allocator&lt;void&gt; &gt;">Predicted momentum-coasting stop pose</output_port>
    </Action>
    <Action ID="PathToPoseVector">
      <output_port name="poses"
                   default="{poses}"
                   type="std::vector&lt;geometry_msgs::msg::PoseStamped_&lt;std::allocator&lt;void&gt; &gt;, std::allocator&lt;geometry_msgs::msg::PoseStamped_&lt;std::allocator&lt;void&gt; &gt; &gt; &gt;">Vector of poses to set</output_port>
      <input_port name="path"
                  default="{path}"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Path to convert to pose vector</input_port>
    </Action>
    <Control ID="PipelineSequence"/>
    <Action ID="PlannerSelector">
      <input_port name="topic_name">Name of the topic to receive planner selection commands</input_port>
      <input_port name="default_planner">Default planner of the planner selector</input_port>
      <output_port name="selected_planner">Name of the selected planner received from the topic subcription</output_port>
    </Action>
    <Decorator ID="RateController">
      <input_port name="hz">Rate</input_port>
    </Decorator>
    <Decorator ID="RestartOnTaskChange">
      <input_port name="task_update_time"
                  type="rclcpp::Time">Timestamp of the current task's last update (wire to {current_task_update_time}); when it changes while the child is RUNNING, the child is halted and re-ticked from the top</input_port>
    </Decorator>
    <Action ID="RemovePassedGoals">
      <input_port name="input_goals">Input goals to remove if passed</input_port>
      <input_port name="radius">Radius tolerance on a goal to consider it passed</input_port>
      <input_port name="global_frame">Global frame</input_port>
      <input_port name="robot_base_frame">Robot base frame</input_port>
      <output_port name="output_goals">Set of goals after removing any passed</output_port>
    </Action>
    <Action ID="SetControllerSpeed">
      <input_port name="speed"
                  type="double">Target speed (m/s). Values &lt;= 0 are treated as "no speed in task" and skip the update; non-finite values (NaN/Inf) are also skipped.</input_port>
      <input_port name="target_node"
                  default="controller_server"
                  type="std::string">Name of the controller server hosting the FollowPath plugin. Relative names are resolved against the BT node's namespace; absolute names (starting with "/") are used as-is.</input_port>
      <input_port name="controller_name"
                  default="FollowPath"
                  type="std::string">Name of the controller plugin whose default_speed is updated. Pair with the BT's selected_controller blackboard variable so the speed update follows whichever controller FollowPath is using.</input_port>
      <input_port name="parameter_suffix"
                  default="default_speed"
                  type="std::string">Parameter suffix appended to controller_name to form the full parameter path (controller_name + "." + parameter_suffix).</input_port>
    </Action>
    <Action ID="AdjustPathForObstacles">
      <input_port name="nominal_path"
                  default="{nominal_survey_path}"
                  type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Nominal trackline to reshape around obstacles</input_port>
      <output_port name="path"
                   default="{survey_path}"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Reshaped path for FollowPath (nominal pass-through when clear/blocked)</output_port>
      <input_port name="costmap_topic"
                  default="local_costmap/costmap_raw"
                  type="std::string">nav2_msgs/Costmap topic supplying the obstacle field</input_port>
      <input_port name="max_xte"
                  default="6.0"
                  type="double">Corridor half-width (m): max deviation and give-up bound</input_port>
      <input_port name="station_step"
                  default="2.0"
                  type="double">Along-track resample spacing (m)</input_port>
      <input_port name="lateral_step"
                  default="0.5"
                  type="double">Cross-track offset resolution (m)</input_port>
      <input_port name="w_xte"
                  default="1.0"
                  type="double">Restoring-spring weight on d^2</input_port>
      <input_port name="w_obs"
                  default="0.02"
                  type="double">Weight on graded obstacle cost</input_port>
      <input_port name="w_smooth"
                  default="2.0"
                  type="double">Smoothness weight on (d_i - d_{i-1})^2</input_port>
      <input_port name="w_temporal"
                  default="0.0"
                  type="double">Chatter-damping weight vs last tick</input_port>
      <input_port name="max_lateral_rate"
                  default="1.0"
                  type="double">Max |d| change between stations (m)</input_port>
      <input_port name="avoid_speed"
                  default="0.0"
                  type="double">Initial speed (m/s) through the avoidance manoeuvre; 0 disables. Seeds live ROS param 'survey_avoidance_speed' (authoritative thereafter)</input_port>
    </Action>
    <Action ID="SetPathFromTask">
      <input_port name="end_index"
                  default="-1"
                  type="int">Index of the last pose in the navigation path</input_port>
      <output_port name="path"
                   default="{current_navigation_path}"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Navigation path to set</output_port>
      <input_port name="start_index"
                  default="0"
                  type="int">Index of the first pose in the navigation path</input_port>
      <input_port name="task"
                  default="{current_task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to get navigation path from</input_port>
    </Action>
    <Action ID="SetPolygonFromTask">
      <output_port name="polygon"
                   type="geometry_msgs::msg::PolygonStamped_&lt;std::allocator&lt;void&gt; &gt;">Polygon to set</output_port>
      <input_port name="end_index"
                  default="-1"
                  type="int">Index of the last pose in the polygon</input_port>
      <input_port name="start_index"
                  default="0"
                  type="int">Index of the first pose describing the polygon</input_port>
      <input_port name="task"
                  default="{current_task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to get polygon from</input_port>
    </Action>
    <Action ID="SetTaskDone">
      <input_port name="task"
                  default="{task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to set as done</input_port>
    </Action>
    <Action ID="SetTaskFailed">
      <input_port name="task"
                  default="{task}"
                  type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Task to record as attempted-but-failed</input_port>
      <input_port name="reason"
                  default=""
                  type="std::string">Short failure reason (e.g. follow_path error code)</input_port>
    </Action>
    <Action ID="SonarCoverage">
      <input_port name="survey_area"
                  default="{survey_area}"
                  type="geometry_msgs::msg::PolygonStamped_&lt;std::allocator&lt;void&gt; &gt;">Polygon representing the area to be surveyed</input_port>
      <output_port name="current_line"
                   default="{current_survey_line}"
                   type="nav_msgs::msg::Path_&lt;std::allocator&lt;void&gt; &gt;">Current survey line</output_port>
      <output_port name="current_line_label"
                   default="{current_line_label}"
                   type="std::string">Current survey line label</output_port>
      <input_port name="server_timeout"
                  type="std::chrono::milliseconds"/>
      <output_port name="current_line_number"
                   default="{current_line_number}"
                   type="short">Current survey line number</output_port>
      <input_port name="server_name"
                  type="std::string">Action server name</input_port>
    </Action>
    <Action ID="UpdateCurrentTask">
      <output_port name="current_task_update_time"
                   default="{current_task_update_time}"
                   type="rclcpp::Time">Time the current task was last updated</output_port>
      <output_port name="current_task_id"
                   default="{current_task_id}"
                   type="std::string">ID of the current task</output_port>
      <output_port name="current_task_type"
                   default="{current_task_type}"
                   type="std::string">Type of the current task</output_port>
      <output_port name="current_task"
                   default="{current_task}"
                   type="std::shared_ptr&lt;marine_nav_tasks::Task&gt;">Pointer to update with current task</output_port>
      <input_port name="task_list"
                  default="{task_list}"
                  type="std::shared_ptr&lt;marine_nav_tasks::TaskList&gt;">List of tasks to check</input_port>
    </Action>
  </TreeNodesModel>

</root>
